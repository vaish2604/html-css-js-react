// Reference an HTML element by its id
let elem = document.getElementById("okay");

// Print its content to the console
console.log(elem.textContent);

// You can also change the text
elem.textContent = "Text updated by JavaScript!";
